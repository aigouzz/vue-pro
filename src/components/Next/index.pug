.next
    .index =============
    .index next get all step
    .sec this is next
    .index =============
    .tags
        .common(v-for="(item, index) in next")
            .title {{item.name}}
            .tag {{item.tag}}