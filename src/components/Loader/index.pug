.loader
    .index ===============
    .index hello，world， {{name}}
    .two yes,this is test
    .three you got
    .index this is load
    .index ==============
    .imgs
        .common(v-for="(item,index) in app")
            .title {{item.title}}
            img(:src="item.img")